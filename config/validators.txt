# ══════════════════════════════════════════════════════════════════════════════
#  validators.txt — Xahau mainnet UNL (the PUBLISHED list, not ours)
#
#  These nodes do not validate. This file only tells them whose validations to
#  trust. It is the published Xahau UNL and has nothing to do with the UNL
#  validator running in CT 200 on the R730xd — that host's keys never appear
#  here or anywhere else in this repo.
#
#  VERIFY the key and site below against the current published Xahau values
#  before first start (xahau.network / the Xahau docs). A stale validator_list
#  key means the node cannot fetch the UNL and will never reach `full`.
#  ops/healthcheck.sh flags a node stuck in `connected`/`syncing`.
# ══════════════════════════════════════════════════════════════════════════════

[validator_list_sites]
https://vl.xahau.org

[validator_list_keys]
ED45D1840EE724BE327ABE9146503D5848EFD5F38B6D5FEDE71E80ACCE5E6E738B
